Node.jsInstallation
Add NodeSource Repository:

curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -

** Install Node.js:**

sudo apt-get install -y nodejs

Verify Installation:

node -v npm -v
Git Installation
Install Git:

sudo apt install git
Verify Installation:
git --version
Clone Your Project Repository
Clone the Repository (Ensure it is public):

git clone git clone https://github.com/************/AWS-Test.git